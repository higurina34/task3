<p id="notice">
    <%= flash[:notice] %>
</p>
<div class="container px-5 px-sm-0">
 <div class="row">
  <div class="col-md-3">
     <h1>User info</h1>

 <p><%= image_tag @user.get_profile_image(100,100) %></p>
 <table class="table table-striped">
    <tbody>
      <tr><h3>name</h3><%= @user.name %></tr>
      <tr><h3>introduction</h3><%= @user.introduction %></tr>
    </tbody>
  </table>

    <%= link_to edit_user_path(@user), class:'btn btn-outline-secondary btn-block edit_user_5553' do %>
    <i class="fas fa-user-cog"></i>

  <% end %>

<h1 class="mt-3">New book</h1>

<%= form_with model: @book_new do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>

<h1>Editing Book</h1>

</div>
<div class="col-md-8 offset-md-1">

<h1>Book detail</h1>
<table class="table">
　<thead>
<tr>
  <th><%= link_to user_path(@user) do %>
      <%= image_tag @book.user.get_profile_image(100,100) %>
      <% end %>
     <%= link_to @user.name, user_path(@user) %></th>

  <th><%= link_to @book.title, book_path(@book) %></th>
  <th><%= @book.body %></th>


  <% if @book.user == current_user %>
    <th><%= link_to "Edit", edit_book_path(@book), class:'btn btn-success' %></th>
    <th><%= link_to "Destroy", book_path(@book), method: :delete, class:'btn btn-danger' %></th>
    </tr>
  <% end %>
</tr>
</thead>
</table>
</div>
</div>
</div>